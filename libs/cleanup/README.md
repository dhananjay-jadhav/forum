# cleanup

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cleanup` to build the library.

## Running unit tests

Run `nx test cleanup` to execute the unit tests via [Jest](https://jestjs.io).
